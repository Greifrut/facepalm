<% if current_user %>
    <div class="page-header"><h1>Добро пожаловать!</h1></div>

    <h2>Некоторая информация для тебя:</h2>

    <div class="well well-lg">
        <ul>
            <li>Количество логинов: <%= current_user.login_count %></li>
            <li>Количество неудавшихся залогиниваний: <%= current_user.failed_login_count %></li>
            <li>Последний заход: <%= current_user.last_request_at %></li>
            <li>Текущий вход в систему: <%= current_user.current_login_at %></li>
            <li>Текущий ip: <%= current_user.current_login_ip %></li>
        </ul>
    </div>
<% else %>
<div class="jumbotron">
        <div class="container">
            <h1>Приветствуем!</h1>
            <p>Зарегестрируйся чтобы начать.</p>
            <p>
                <%= link_to new_user_path, class: 'btn btn-primary btn-lg' do %>
                Зарегестрироваться &raquo;
                <% end %>
            </p>
        </div>
    </div>
<% end %>