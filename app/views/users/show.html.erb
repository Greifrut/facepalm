<h1><%= @user.email %></h1>
<%= render 'friends_form'%>
<div class="span8">
    <% if @user.posts.any? %>
        <h3>Post (<%= @user.posts.count %>)</h3>
        <%= render @posts %>
    <% end %>
</div>