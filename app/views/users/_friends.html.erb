<%= form_for(current_user.active_friendships.build) do |f| %>
    <div><%= hidden_field_tag :requester_id, @user.id %></div>
    <%= f.submit "Добавить в друзья", class: "btn btn-primary" %>
<% end %>